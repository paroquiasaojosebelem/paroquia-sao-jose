import Header from '../../../../components/Header'
import Footer from '../../../../components/Footer'
import {createClient} from '../../../../lib/supabase/server'
const roles={commentator:'Comentarista',first_reading:'1ª Leitura',second_reading:'2ª Leitura',psalmist:'Salmista'}
const fmt=s=>new Intl.DateTimeFormat('pt-BR',{weekday:'long',day:'2-digit',month:'long'}).format(new Date(`${s}T12:00:00`))
export default async function Page(){const s=await createClient();const {data:cycles=[]}=await s.from('liturgy_availability_cycles').select('*').eq('status','published').order('month',{ascending:false}).limit(1);const cycle=cycles[0];let rows=[];if(cycle){const q=await s.from('liturgy_assignments').select('*,liturgy_members(name),mass_schedule(title,mass_time)').eq('cycle_id',cycle.id).order('mass_date');rows=q.data||[]}const grouped={};for(const a of rows){const key=`${a.mass_date}|${a.mass_schedule_id}`;(grouped[key]??=[]).push(a)}return <><Header/><main className="wrap page"><div className="pageHero centered"><span className="eyebrow dark">PASTORAL DA LITURGIA</span><h1>Escala Litúrgica</h1><p>Serviço de comentaristas, leitores e salmistas nas celebrações da comunidade.</p></div>{!cycle?<div className="panel centered"><h2>Escala em preparação</h2><p>A próxima escala será publicada aqui assim que for concluída pela coordenação.</p></div>:<><div className="panel schedulePublicHeader"><h2>{new Intl.DateTimeFormat('pt-BR',{month:'long',year:'numeric'}).format(new Date(`${cycle.month.slice(0,7)}-01T12:00:00`))}</h2><p>Em caso de impossibilidade, procure a coordenação da Pastoral da Liturgia com antecedência.</p><button className="ghost printBtn" onClick={undefined}>Use Ctrl+P para imprimir / salvar em PDF</button></div><div className="publicScheduleGrid">{Object.entries(grouped).map(([key,items])=>{const x=items[0];return <article className="publicScheduleCard" key={key}><h3>{fmt(x.mass_date)}</h3><p className="massTitle">{x.mass_schedule?.mass_time?.slice(0,5)} — {x.mass_schedule?.title}</p>{items.map(a=><div className="publicAssignment" key={a.id}><span>{roles[a.role]}</span><b>{a.liturgy_members?.name||'Vaga em aberto'}</b></div>)}</article>})}</div></>}</main><Footer/></>}
