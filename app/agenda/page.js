import Header from '../../components/Header';import Footer from '../../components/Footer';import {createClient} from '../../lib/supabase/server'
const fmt=v=>new Intl.DateTimeFormat('pt-BR',{weekday:'long',day:'2-digit',month:'long',year:'numeric',hour:'2-digit',minute:'2-digit'}).format(new Date(v))
export default async function Agenda(){const s=await createClient();const now=new Date().toISOString();const {data=[]}=await s.from('parish_events').select('*').eq('active',true).or(`ends_at.gte.${now},and(ends_at.is.null,starts_at.gte.${now})`).order('starts_at');return <><Header/><main className="wrap page"><div className="pageHero"><span className="eyebrow dark">VIDA PAROQUIAL</span><h1>Agenda Paroquial</h1><p>Veja os próximos encontros, celebrações, formações e atividades da nossa comunidade.</p></div>{data.length?<div className="agendaGrid">{data.map(e=><article className="agendaCard" key={e.id}>{e.image_url&&<img src={e.image_url} alt=""/>}<div className="agendaCardBody"><small>{e.category||'EVENTO'}</small><h2>{e.title}</h2><b className="agendaDate">{fmt(e.starts_at)}</b>{e.location&&<p>📍 {e.location}</p>}{e.summary&&<p>{e.summary}</p>}{e.responsible&&<small>Responsável: {e.responsible}</small>}{e.description&&<details><summary>Ver detalhes</summary><p>{e.description}</p></details>}</div></article>)}</div>:<div className="panel"><p>Não há eventos futuros publicados neste momento.</p></div>}</main><Footer/></>}
