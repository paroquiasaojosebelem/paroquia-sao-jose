import Header from '../../components/Header';import Footer from '../../components/Footer';import YouTubePlayer from '../../components/YouTubePlayer';import {createClient} from '../../lib/supabase/server'
export default async function Transmissao(){const s=await createClient();const {data=[]}=await s.from('live_streams').select('*').eq('active',true).order('starts_at',{ascending:true});const now=Date.now();const live=data.find(x=>x.status==='live')||data.find(x=>new Date(x.starts_at).getTime()>=now)||data[0];const upcoming=data.filter(x=>x.id!==live?.id&&new Date(x.starts_at).getTime()>=now).slice(0,6);const past=data.filter(x=>x.status==='ended'||new Date(x.starts_at).getTime()<now).slice(-6).reverse();return <><Header/><main className="wrap page"><div className="pageHero centered"><span className="liveBadge">● TRANSMISSÃO</span><h1>Ao Vivo</h1><p>Acompanhe as Santas Missas e celebrações da Paróquia São José.</p></div>{live?<section className="liveMain"><YouTubePlayer url={live.youtube_url} title={live.title}/><div className="liveInfo"><span className={live.status==='live'?'liveNow':'scheduled'}>{live.status==='live'?'● AO VIVO':'PRÓXIMA TRANSMISSÃO'}</span><h2>{live.title}</h2><p>{live.description}</p><b>{live.starts_at?new Date(live.starts_at).toLocaleString('pt-BR',{dateStyle:'full',timeStyle:'short'}):''}</b></div></section>:<section className="panel centered"><h2>Neste momento não há transmissão cadastrada.</h2><p>A Pascom poderá programar a próxima celebração pelo painel administrativo.</p></section>}<div className="twoCols liveLists"><section><h2>Próximas transmissões</h2>{upcoming.length?upcoming.map(x=><article className="streamCard" key={x.id}><b>{x.title}</b><span>{new Date(x.starts_at).toLocaleString('pt-BR',{dateStyle:'short',timeStyle:'short'})}</span></article>):<p>Nenhuma transmissão futura cadastrada.</p>}</section><section><h2>Celebrações anteriores</h2>{past.length?past.map(x=><article className="streamCard" key={x.id}><b>{x.title}</b><a href={x.youtube_url} target="_blank" rel="noreferrer">Assistir novamente →</a></article>):<p>O histórico aparecerá aqui após as primeiras transmissões.</p>}</section></div></main><Footer/></>}
