import {revalidatePath} from 'next/cache';import {requireAdmin} from '../../../lib/auth'
async function saveDonation(fd){'use server';const {supabase}=await requireAdmin();const payload={pix_key:fd.get('pix_key')?.trim()||null,pix_qr_url:fd.get('pix_qr_url')?.trim()||null,card_payment_url:fd.get('card_payment_url')?.trim()||null,active:true};const {data}=await supabase.from('donation_settings').select('id').limit(1).maybeSingle();if(data?.id)await supabase.from('donation_settings').update(payload).eq('id',data.id);else await supabase.from('donation_settings').insert(payload);revalidatePath('/dizimo');revalidatePath('/admin/configuracoes')}
export default async function Page(){const {supabase}=await requireAdmin();const {data:cfg}=await supabase.from('donation_settings').select('*').limit(1).maybeSingle();return <><div className="adminTitle"><h1>Configurações</h1><p>Dados institucionais e meios de contribuição.</p></div><section className="panel adminPanel"><h2>Dízimo e contribuições</h2><form action={saveDonation} className="adminForm"><label>Chave PIX<input name="pix_key" defaultValue={cfg?.pix_key||''}/></label><label>Imagem / QR Code PIX (URL)<input name="pix_qr_url" defaultValue={cfg?.pix_qr_url||''}/></label><label>Link seguro para cartão de débito/crédito<input name="card_payment_url" defaultValue={cfg?.card_payment_url||''} placeholder="Link Mercado Pago, PagSeguro ou outro provedor"/></label><button>Salvar configurações</button></form><p className="adminHint">O site não guarda número de cartão. O botão direciona o fiel para o ambiente seguro do provedor de pagamento.</p></section></>}
